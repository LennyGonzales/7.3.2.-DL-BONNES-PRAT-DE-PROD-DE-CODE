services:
  app:
    build: .
    container_name: express-sqlite-app
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    environment:
      - NODE_ENV=development
    command: npm run dev
