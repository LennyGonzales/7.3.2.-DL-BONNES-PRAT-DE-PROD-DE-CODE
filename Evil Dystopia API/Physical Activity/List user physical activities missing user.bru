meta {
  name: List user physical activities missing user
  type: http
  seq: 35
}

get {
  url: http://localhost:3000/users/:user_id/physical_activities
  body: none
  auth: none
}

params:path {
  user_id:
}

tests {
  test('status', function () {
    expect(res.status).to.equal(404);
  });

  test('error message', function () {
    expect(res.body).to.contain('Cannot GET');
  });

}
